node_modules/
dist/
build/
.react-router/

# Les secrets : jamais dans le dépôt. Voir server/.env.example.
.env

# L'ancienne base SQLite des semaines 1 à 4, si elle traîne encore.
server/data/*.db
server/data/*.db-*
